2. 소문자를 대문자로 변환하는 함수를 작성하시오.
함수 입력 : 한 개의 소문자
함수 출력 : 입력이 알파벳 중 하나이면, 해당하는 대문자, 그렇지 않으면 ????? 표시

-----------------------------------------------------------------------------------------------------------------

# include <stdio.h>
# include <string.h>


void upper(char i)
{
	if (i >= 'a' && i <= 'z')
	{
		i -= 32;
		printf("변환된 문자열은 %c 입니다", i);
	}
	else if (i >= 'A' && i <= 'Z')
	{
		printf("변환된 문자열은 %c 입니다", i);
	}
	else
	{
		printf("????????????");
		
	}
}

int main(void)
{
	char x;
	printf("문자열을 입력하세요 : ");
	scanf_s("%c", &x, sizeof(x));
	upper(x);
	return 0;
}
