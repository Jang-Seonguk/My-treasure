첫 번째 문제 :  *****      그리기
                ***
                 *


----------------------------------------------------------------------------------------------------------
                 
# include <stdio.h>

int main(void)
{
	char x[10] = "* ";
	char y[10] = "  ";
	for (int i = 1; i <= 5; i += 2)
	{
		for (int j = (5 - i) / 2; j >= 0; j -= 1)
		{
			printf("%s", y);
		}
		for (int j = 1; j <= i; j++)
		{
			printf("%s", x);
		}
		for (int j = (5 - i) / 2; j >= 0; j -= 1)
		{
			printf("%s", y);
		}
		printf("\n");
	}
	return 0;
}         
----------------------------------------------------------------------------------------------------------

두 번째 문제 : 사용자에게 라인 수를 입력받아서 그 줄 수 만큼 별표 삼각형을 만드시오.
 사용자 입력: 4
     *
    ***
   *****
  *******

----------------------------------------------------------------------------------------------------------

# include <stdio.h>

int main(void)
{
	int nu;
	int count = 0;
	char x[10] = "* ";
	char y[10] = "  ";
	printf("원하시는 라인 수를 입력하세요 : ");
	scanf_s("%d", &nu, sizeof(nu));
	count = (2 * nu) - 1;
	for (int i = 1; i <= count; i += 2)
	{
		for (int j = 0; j < (count - i) / 2; j++)
		{
			printf("%s", y);
		}
		for (int j = 0; j < i; j++)
		{
			printf("%s", x);
		}
		for (int j = 0; j < (count - i) / 2; j++)
		{
			printf("%s", y);
		}
		printf("\n");
	}
	return 0;
}
